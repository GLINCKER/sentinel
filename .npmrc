# pnpm Configuration for Sentinel
# Built by Glincker (A GLINR Product)

# Use strict peer dependencies (pnpm default)
auto-install-peers=true
strict-peer-dependencies=false

# Hoist node_modules (faster builds)
shamefully-hoist=true

# Save exact versions
save-exact=false

# Public registry
registry=https://registry.npmjs.org/
